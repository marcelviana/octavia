---
phase: 02-fix-chords-content-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/performance-mode.tsx
  - hooks/use-content-caching.ts
autonomous: true
requirements: [BUG-01]

must_haves:
  truths:
    - "Chords content displays sections without duplication in performance mode"
    - "Section names (Verse 1, Chorus, Verse 2) render once per section"
    - "Chord progressions (C F G Am) display once per section"
    - "Section lyrics display correctly alongside chords"
    - "Other content types (Lyrics, Tabs, Piano, Drums) remain unaffected"
  artifacts:
    - path: "components/performance-mode.tsx"
      provides: "Clean data flow from selectedContent to songs array"
      min_lines: 174
    - path: "hooks/use-content-caching.ts"
      provides: "Proper chordsData extraction without duplication"
      min_lines: 187
  key_links:
    - from: "components/performance-mode.tsx"
      to: "hooks/use-content-caching.ts"
      via: "songs array prop"
      pattern: "useContentCaching\\(\\{ songs \\}\\)"
    - from: "hooks/use-content-caching.ts"
      to: "hooks/use-content-renderer.ts"
      via: "chordsData array"
      pattern: "chordsData\\[currentSong\\]"
    - from: "hooks/use-content-renderer.ts"
      to: "components/performance-mode/content-display.tsx"
      via: "contentRenderInfo prop"
      pattern: "renderInfo\\.chordsData"
---

<objective>
Fix duplicate rendering of Chords content sections in performance mode by correcting the data flow integration layer between parent component and hooks.

Purpose: Ensure musicians see chord charts displayed correctly during live performances without duplicated sections confusing their reading.

Output: Clean chords rendering with single occurrence of each section (Verse 1, Chorus, etc.) and passing integration tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-fix-chords-content-display/02-RESEARCH.md
@.planning/phases/01-diagnostic-test-foundation/01-03-SUMMARY.md
@CLAUDE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md

# Current implementation files
@components/performance-mode.tsx
@hooks/use-content-caching.ts
@hooks/use-content-renderer.ts
@components/performance-mode/content-display.tsx

# Test files that will verify the fix
@__tests__/performance-mode/chords-display-bug.test.tsx
@hooks/__tests__/use-content-renderer.test.ts
@components/__tests__/content-display.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add data flow debugging and identify exact duplication point</name>
  <files>
    components/performance-mode.tsx
    hooks/use-content-caching.ts
  </files>
  <action>
Add strategic console.log statements at each data transformation point to trace where sections array is being duplicated:

1. In `components/performance-mode.tsx` after `songs` array construction (line 88):
   - Log the structure of `content_data.sections` for first song
   - Verify sections array appears only once in the constructed object

2. In `hooks/use-content-caching.ts` after `chordsData` extraction (line 59):
   - Log the chordsData array structure
   - Verify sections count matches expected (should be 3 sections for test data)

3. Run the failing integration test: `pnpm test __tests__/performance-mode/chords-display-bug.test.tsx --run`

4. Analyze console output to identify where duplication occurs:
   - If sections appear twice in performance-mode.tsx logs → bug is in songs construction (lines 36-88)
   - If sections appear once in performance-mode.tsx but twice in use-content-caching.ts → bug is in extraction logic (lines 53-59)
   - If sections appear once in both hooks but test still fails → bug is in component rendering (unlikely per unit tests)

DO NOT FIX THE BUG YET - this task is diagnostic only. Document findings in console output for Task 2.

Note: Phase 1 diagnostic tests proved both hook and component work correctly in isolation, so bug MUST be in integration layer data flow.
  </action>
  <verify>
    <automated>pnpm test __tests__/performance-mode/chords-display-bug.test.tsx --run 2>&1 | tee /tmp/phase2-debug.log</automated>
    <manual>Review console output in /tmp/phase2-debug.log to identify exact line where duplication occurs</manual>
    <sampling_rate>run once after task commits</sampling_rate>
  </verify>
  <done>
Console logs clearly show at which transformation point (songs construction or chordsData extraction) the sections array gets duplicated. Debug output saved to /tmp/phase2-debug.log for analysis in Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix data flow duplication based on Task 1 findings</name>
  <files>
    components/performance-mode.tsx
    hooks/use-content-caching.ts
  </files>
  <action>
Based on Task 1 console output, apply the appropriate fix:

**If duplication is in performance-mode.tsx (lines 36-88):**
- Review the sections extraction logic in the map function
- Check if sections are being extracted multiple times (e.g., both in root content_data AND nested again)
- Ensure each Content object's content_data.sections is extracted exactly once
- Likely issue: The ternary operator pattern for sections (lines 56-58, 81-83) may be incorrectly structured
- Fix: Verify that `s.content.content_data.sections` and `selectedContent.content_data.sections` are not being duplicated in the object construction

**If duplication is in use-content-caching.ts (lines 53-59):**
- Review the chordsData useMemo extraction
- Check if sections are being processed twice in the map function
- Ensure `song?.content_data?.sections` returns a single array reference, not duplicated
- Fix: Verify the extraction returns the sections array directly without transforming it

**Common fix patterns from research:**
- Use TypeScript type guards to ensure only one extraction path executes: `if ('sections' in data && Array.isArray(data.sections))`
- Avoid inline object creation that creates new references
- Ensure useMemo dependencies are correct to prevent stale data

**Anti-patterns to avoid (from RESEARCH.md Pitfall #1):**
- Don't mutate the content_data object directly
- Don't process the same data twice in nested ternaries
- Don't use type assertions (`as any`) that hide data structure issues

After applying fix, remove all console.log debug statements added in Task 1.

Commit with message: `fix(02-01): eliminate duplicate sections in chords data flow`
  </action>
  <verify>
    <automated>pnpm test __tests__/performance-mode/chords-display-bug.test.tsx --run</automated>
    <manual>All 3 integration tests should pass (currently 2/3 failing). Verify no "Found multiple elements" errors in test output.</manual>
    <sampling_rate>run after this task commits, before Task 3 begins</sampling_rate>
  </verify>
  <done>
Integration tests pass (3/3) with no duplicate element errors. Chords sections render once per section. Console.log debug statements removed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify no regressions in other content types</name>
  <files>
    __tests__/performance-mode/chords-display-bug.test.tsx
    hooks/__tests__/use-content-renderer.test.ts
    components/__tests__/content-display.test.tsx
  </files>
  <action>
Run comprehensive test suite to ensure the fix didn't break other content types or introduce new bugs:

1. Run full unit test suite for hooks and components:
   `pnpm test hooks/__tests__/use-content-renderer.test.ts components/__tests__/content-display.test.tsx --run`
   - All hook unit tests (5/5) should still pass
   - All component unit tests (7/7) should still pass

2. Run integration test suite:
   `pnpm test __tests__/performance-mode/ --run`
   - Chords display tests (3/3) should pass
   - Auto-scroll button test (1/1) should pass

3. If any tests fail:
   - Review the failing test output
   - Identify if failure is related to the chords fix
   - If related: adjust the fix in Task 2 to be more surgical
   - If unrelated: document in SUMMARY as pre-existing failure

4. Verify rendering for different content type scenarios:
   - Lyrics content (renderType: 'lyrics')
   - Sheet music (renderType: 'pdf', 'image')
   - Tabs content (renderType: 'chords' - shares code with Chords)
   - Empty content (renderType: 'no-lyrics', 'no-sheet')

NO code changes expected in this task unless regression found. This is verification only.

Note: Per RESEARCH.md Pitfall #2, breaking other content types is a known risk. This task mitigates that risk.
  </action>
  <verify>
    <automated>pnpm test __tests__/performance-mode/ hooks/__tests__/use-content-renderer.test.ts components/__tests__/content-display.test.tsx --run</automated>
    <manual>Review test output summary: expect all tests passing (3 chords + 1 auto-scroll + 5 hook + 7 component = 16 tests total)</manual>
    <sampling_rate>run after task completes</sampling_rate>
  </verify>
  <done>
All 16 tests pass. No regressions introduced. Chords fix is surgical and doesn't affect Lyrics, Sheet, Tabs, or empty content rendering. Performance mode renders all content types correctly.
  </done>
</task>

</tasks>

<verification>
Run full phase verification after all tasks complete:

1. **Integration tests pass:**
   ```bash
   pnpm test __tests__/performance-mode/chords-display-bug.test.tsx --run
   ```
   Expected: 3/3 tests passing (currently 2/3 failing)

2. **Unit tests still pass:**
   ```bash
   pnpm test hooks/__tests__/use-content-renderer.test.ts components/__tests__/content-display.test.tsx --run
   ```
   Expected: 12/12 tests passing (5 hook + 7 component)

3. **No duplicate elements in DOM:**
   Test output should not contain "Found multiple elements" error

4. **Visual verification (manual if needed):**
   - Launch performance mode with Chords content
   - Verify each section (Verse 1, Chorus, Verse 2) appears exactly once
   - Verify chord progressions display correctly
   - Verify section lyrics display without duplication
</verification>

<success_criteria>
- [x] Integration test "should display full chord chart in performance mode" passes
- [x] Integration test "should display section lyrics along with chords" passes
- [x] Integration test "should handle multiple chord chart sections" passes
- [x] All unit tests remain passing (12/12 hook + component tests)
- [x] No "Found multiple elements" error in test output
- [x] Data flow from performance-mode.tsx → use-content-caching.ts → use-content-renderer.ts → content-display.tsx is clean
- [x] Sections array extracted exactly once at each transformation point
- [x] Other content types (Lyrics, Tabs, Sheet) render without regression
- [x] Console.log debug statements removed from production code
</success_criteria>

<output>
After completion, create `.planning/phases/02-fix-chords-content-display/02-01-SUMMARY.md` documenting:
- Exact location where duplication occurred (identified in Task 1)
- Specific fix applied (implemented in Task 2)
- Test results before/after (2/3 failing → 3/3 passing)
- Any edge cases discovered during fix
- Verification that no regressions introduced
</output>
