---
phase: 04-memory-leak-performance-optimization
plan: 03
type: execute
wave: 2
depends_on: ["02"]
files_modified:
  - components/optimized-performance-mode.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "optimized-performance-mode.tsx component is under 150 lines"
    - "All business logic extracted to hooks or sub-components"
    - "Component renders correctly with all extractions"
  artifacts:
    - path: "components/optimized-performance-mode.tsx"
      provides: "Parent orchestration component"
      max_lines: 150
  key_links:
    - from: "components/optimized-performance-mode.tsx"
      to: "extracted hooks"
      via: "hook composition pattern"
      pattern: "use[A-Z]\\w+"
---

<objective>
Complete component refactoring by performing final extractions to bring optimized-performance-mode.tsx from ~280 lines (after Plan 02) to under 150 lines.

Purpose: Achieve full CLAUDE.md <150 line compliance through targeted extractions of remaining complex logic blocks. After Plan 02 removes 98 lines, component is at ~280 lines and needs ~130 more lines removed through memoization consolidation, keyboard logic extraction, and other refactorings.

Output: Component under 150 lines with all business logic extracted; full CLAUDE.md compliance; existing tests passing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-memory-leak-performance-optimization/04-RESEARCH.md
@.planning/phases/04-memory-leak-performance-optimization/04-02-SUMMARY.md
@components/optimized-performance-mode.tsx
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify component structure and identify extraction candidates</name>
  <files>components/optimized-performance-mode.tsx</files>
  <action>
Read the current component structure after Plan 02 refactoring and verify actual line numbers and extraction candidates.

**Verification steps:**

1. Read components/optimized-performance-mode.tsx
2. Count actual lines: `wc -l components/optimized-performance-mode.tsx`
3. Identify remaining large blocks:
   - Memoization setup (lines 56-77 estimated, ~22 lines)
   - Keyboard event handlers (if separate from usePerformanceControls, ~25 lines estimated)
   - Wake lock logic (if present, ~35 lines estimated)
   - Viewport/fullscreen setup (if present, ~40 lines estimated)

4. Calculate extraction targets:
   - Current: ~280 lines (post Plan 02)
   - Target: <150 lines
   - Need to remove: ~130 lines minimum

5. Prioritize extractions by impact:
   - Priority 1: Largest blocks first (viewport, wake lock if they exist)
   - Priority 2: Memoization consolidation (convert separate useMemo to inline)
   - Priority 3: Import consolidation and whitespace optimization

**Output verification plan to console with:**
- Actual current line count
- Confirmed extraction targets with line ranges
- Expected line count after each extraction
- Final projected line count

If component structure differs significantly from research estimates, pause and report findings for plan adjustment.
  </action>
  <verify>
    <automated>test -f components/optimized-performance-mode.tsx</automated>
    <manual>Component structure verified; extraction candidates identified with actual line ranges; verification plan documented showing path to <150 lines</manual>
    <sampling_rate>run immediately before extractions</sampling_rate>
  </verify>
  <done>Component structure analyzed; actual extraction candidates identified with line ranges; verification plan shows achievable path to <150 lines</done>
</task>

<task type="auto">
  <name>Task 2: Perform targeted extractions to reach under 150 lines</name>
  <files>components/optimized-performance-mode.tsx</files>
  <action>
Based on Task 1 verification, perform targeted extractions in priority order to reach <150 lines.

**Extraction strategy (apply in order of impact):**

**1. Consolidate memoization setup (~22 lines)**
- Convert separate useMemo calls (lines 56-77 estimated) to inline props
- Remove unnecessary memoization variables if React.memo already applied
- Pass props directly to memoized components (let React.memo handle optimization)

**2. Extract keyboard event handlers (if not in usePerformanceControls)**
- If keyboard logic is separate, extract to dedicated hook
- Move event listener setup/teardown to hook
- Keep only hook call in component

**3. Extract wake lock logic (if present)**
- Move wake lock acquisition/release to dedicated hook
- Encapsulate Screen Wake Lock API calls
- Return only status from hook

**4. Extract viewport/fullscreen setup (if present)**
- Move viewport detection and fullscreen handling to hook
- Encapsulate document.fullscreenElement logic
- Return only relevant state

**5. Optimize imports and structure**
- Consolidate related imports
- Remove unused imports
- Reduce unnecessary whitespace while maintaining readability

**After each extraction:**
```bash
wc -l components/optimized-performance-mode.tsx
```

**Target component structure:**
```typescript
export const OptimizedPerformanceMode = memo(function OptimizedPerformanceMode({
  onExitPerformance,
  selectedContent,
  selectedSetlist,
  startingSongIndex = 0,
}: PerformanceModeProps) {
  // All business logic extracted to hooks (~10-20 lines of hook calls)
  const contentData = useContentLoading(songs)
  const monitoringUI = usePerformanceMonitoringUI(summary, alerts)
  const { measureNavigation, measureRender, summary, alerts } = usePerformanceMonitoring()
  const navigation = usePerformanceNavigation(...)
  const controls = usePerformanceControls(...)
  const renderer = useContentRenderer(...)

  // Pure rendering logic only (~100-120 lines of JSX)
  return (
    <div className="...">
      <MemoizedHeaderControls {...headerProps} />
      <MemoizedOptimizedContentDisplay {...contentProps} />
      <MemoizedNavigationControls {...navProps} />
      {monitoringUI.showWarning && <WarningDisplay />}
    </div>
  )
})
```

**Stop condition:** Once `wc -l` shows <150 lines, stop extracting. Do not over-optimize.

**Preserve functionality:**
- All existing tests must pass
- React.memo optimization maintained
- Props passed correctly to child components
- Event handlers maintain same behavior
  </action>
  <verify>
    <automated>pnpm test components/optimized-performance-mode && test $(wc -l < components/optimized-performance-mode.tsx) -lt 150</automated>
    <manual>Component under 150 lines verified via wc -l; all business logic extracted; existing performance mode tests pass (6/6); React.memo preserved; functionality unchanged</manual>
    <sampling_rate>run after this task commits, before phase completion</sampling_rate>
  </verify>
  <done>Component refactored to under 150 lines; all targeted extractions completed; existing tests passing; CLAUDE.md <150 line requirement met; functionality unchanged</done>
</task>

</tasks>

<verification>
Verify component size compliance:
```bash
wc -l components/optimized-performance-mode.tsx
```
Expected: Under 150 lines

Run full performance mode test suite:
```bash
pnpm test performance-mode
```
Expected: All 6 tests passing

Verify no regressions:
```bash
pnpm test:all
```

Check TypeScript compilation:
```bash
pnpm tsc --noEmit
```
Expected: No errors
</verification>

<success_criteria>
- [ ] optimized-performance-mode.tsx is under 150 lines (CLAUDE.md compliance)
- [ ] All business logic extracted to hooks or sub-components
- [ ] Component maintains pure rendering focus
- [ ] All existing performance mode tests pass (6/6)
- [ ] Component maintains all existing functionality (memoization, props, rendering)
- [ ] No TypeScript errors or `any` types introduced
- [ ] React.memo optimization preserved
- [ ] PERF-02 requirement fully addressed
</success_criteria>

<output>
After completion, create `.planning/phases/04-memory-leak-performance-optimization/04-03-SUMMARY.md`
</output>
