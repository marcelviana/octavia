> **Rastro** da Fase A do pre-check do N3 (subagente de leitura, 2026-09-23, worktree `../octavia-n3-pre` em `aa91b5d`). A fonte é o `N3-PRECHECK.md` §1; onde divergirem, vale o documento.

# A1 — Como o app lê largura hoje

Fase A, só leitura. Worktree `/Users/marcelviana/projects/octavia-n3-pre`, branch `n3/precheck`, HEAD `aa91b5d` (= origin/main).

```
$ git status | head -3 && git log --oneline -1
On branch n3/precheck
Your branch is up to date with 'origin/main'.

aa91b5d Merge pull request #323 from marcelviana/w4b/palco
```

## 1.1 A varredura pedida

[medido]
```
$ grep -rn "useWindowDimensions\|Dimensions\.\|onLayout\|orientation" apps/native/src packages/core/src
apps/native/src/screens/EndScreen.tsx:114:      <View style={styles.meio} onLayout={medirMeio}>
apps/native/src/screens/ModoDeReordenar.tsx:596:          onLayout={(e) => {
apps/native/src/screens/StageScreen.tsx:37:  useWindowDimensions,
apps/native/src/screens/StageScreen.tsx:212:  const { width, height } = useWindowDimensions()
apps/native/src/screens/StageScreen.tsx:219:   * Sai só na MUDANÇA de orientação, não na montagem: o `useWindowDimensions`
apps/native/src/screens/StageScreen.tsx:458:  // duas medidas vêm do `onLayout` do `meio`, não de `useWindowDimensions()`.
apps/native/src/screens/StageScreen.tsx:460:  // Por quê (V1-PRECHECK §3.4, div. 16/24): `useWindowDimensions()` devolve a
apps/native/src/screens/StageScreen.tsx:466:  // E por que `onLayout` e não `useSafeAreaInsets()`: o `SafeAreaView` da raiz
apps/native/src/screens/StageScreen.tsx:469:  // que alguém tocar em `edges`. O `onLayout` mede a altura que a View de fato
apps/native/src/screens/StageScreen.tsx:573:        {/* No PRIMEIRO frame o `onLayout` ainda não disparou e as bordas não
```

Conclusões diretas da saída:
- **`packages/core/src` não lê dimensão nenhuma** (zero linhas).
- **`Dimensions.` (API imperativa: `Dimensions.get` / `Dimensions.addEventListener`) não aparece em lugar nenhum** — não há leitura estática de tela no escopo de módulo.
- Só **três arquivos** leem medida: `StageScreen.tsx`, `EndScreen.tsx`, `ModoDeReordenar.tsx`. Linhas 219, 458, 460, 466, 469, 573 do StageScreen são comentários.

Varredura complementar (não pedida, para fechar o universo — outras formas de ler tamanho):

[medido]
```
$ grep -rn "measure\|useSafeAreaInsets\|PixelRatio\|isPad\|isTablet\|SafeAreaView\|edges=" apps/native/src apps/native/App.tsx packages/core/src
apps/native/src/screens/ModoDeReordenar.tsx:342:      corpoRef.current?.measureInWindow?.((_x, y, _w, h) => {
apps/native/src/screens/StageScreen.tsx:466:  // E por que `onLayout` e não `useSafeAreaInsets()`: o `SafeAreaView` da raiz
apps/native/App.tsx:13:import { SafeAreaProvider, SafeAreaView } from 'react-native-safe-area-context'
apps/native/App.tsx:295:      <SafeAreaView style={styles.raiz} edges={['top', 'bottom', 'left', 'right']}>
apps/native/App.tsx:332:      </SafeAreaView>

$ grep -rn "useWindowDimensions\|Dimensions\.\|onLayout\|orientation" apps/native/App.tsx apps/native/index.ts
(saída vazia)
```

## 1.2 Leitura por arquivo

### StageScreen.tsx (S3, palco) — duas leituras distintas

**(a) `useWindowDimensions()` — `StageScreen.tsx:212`**, lê `width` e `height` da janela/tela.

[medido]
```
212:  const { width, height } = useWindowDimensions()
...
224:  const orientacaoRef = useRef<'landscape' | 'portrait' | null>(null)
225:  const orientacao: 'landscape' | 'portrait' = width >= height ? 'landscape' : 'portrait'
226:
227:  useEffect(() => {
228:    const anterior = orientacaoRef.current
229:    orientacaoRef.current = orientacao
230:    if (anterior === null || anterior === orientacao) return
231:    log(`rotation=${orientacao} n=${posicaoRef.current}/${totalRef.current}`)
232:  }, [orientacao])
```
- Uso: **só para o log** `rotation=landscape|portrait n=<i>/<N>` (T1-R27 / A14, comentário `StageScreen.tsx:214-223`). Não entra em nenhum estilo.

[medido] (todas as ocorrências de `width`/`height` no arquivo)
```
$ grep -n "\bwidth\b\|\bheight\b" StageScreen.tsx | head -30
212:  const { width, height } = useWindowDimensions()
225:  const orientacao: 'landscape' | 'portrait' = width >= height ? 'landscape' : 'portrait'
467:  // (`App.tsx:206`) já consome os quatro insets, então `height - insets.top -
473:    const { width: w, height: h } = e.nativeEvent.layout
580:              style={[styles.borda, { width: larguraBorda, height: alturaConteudo, left: 0 }]}
585:              style={[styles.borda, { width: larguraBorda, height: alturaConteudo, right: 0 }]}
734:           * `fitPolicy={0}` = **fit width**, a partitura ocupando a largura.
743:           * LIMITAÇÃO CONHECIDA, registrada: com fit width o deslize rola
881:    height: bar.top,
908:  pontoOffline: { width: 8, height: 8, borderRadius: 4, backgroundColor: dark.offline },
912:  pdf: { flex: 1, width: '100%' },
917:    height: touch.stage,
940:    height: bar.stage,
950:    width: touch.stage + 2,
951:    height: touch.stage + 2,
```
`width`/`height` do hook aparecem só em 212 e 225.
- **Reage à rotação: SIM.** `useWindowDimensions` é hook que re-renderiza a cada mudança de dimensão (o próprio comentário 219-220 diz: "o `useWindowDimensions` re-renderiza a cada giro"). O `useEffect` depende de `orientacao`, então dispara no giro, e o ref suprime a primeira execução (montagem).
- Observação: a classificação é `width >= height` — um aparelho quadrado conta como paisagem. Não há faixa de largura; é binária.

**(b) `onLayout` do `meio` — `StageScreen.tsx:471-477` e `530`**, lê `width` e `height` da View `styles.meio` (a área entre a barra superior e a inferior).

[medido]
```
471:  const [meio, setMeio] = useState<{ largura: number; altura: number } | null>(null)
472:  const medirMeio = useCallback((e: LayoutChangeEvent) => {
473:    const { width: w, height: h } = e.nativeEvent.layout
474:    setMeio((m) => (m !== null && m.largura === w && m.altura === h ? m : { largura: w, altura: h }))
475:  }, [])
476:  const alturaConteudo = meio === null ? 0 : Math.max(meio.altura, touch.min)
477:  const larguraBorda = meio === null ? 0 : Math.max(meio.largura * 0.15, touch.min)
...
530:      <View style={styles.meio} onLayout={medirMeio}>
...
578:        {meio !== null ? (
580:              style={[styles.borda, { width: larguraBorda, height: alturaConteudo, left: 0 }]}
585:              style={[styles.borda, { width: larguraBorda, height: alturaConteudo, right: 0 }]}
```
- Uso: **as zonas de toque laterais** (voltar/avançar) = `max(15% da largura do meio, touch.min)` × `max(altura do meio, touch.min)`. `touch.min` = 48 (ver A2, `theme.ts`).
- **Reage à rotação: SIM.** `onLayout` dispara sempre que o layout da View muda (o giro muda largura e altura do `meio`); o `setMeio` só troca o estado se os números mudaram. É **a única leitura de largura que muda geometria** no app inteiro.
- Justificativa registrada no código (458-470): V1-PRECHECK §3.4, div. 16/24 — `useWindowDimensions` devolvia a TELA (711,1 dp no AVD) e não a janela (627,1 dp), transbordando 84,0 dp; por isso a medida passou para `onLayout`.
- Achado lateral: o comentário em `StageScreen.tsx:467` cita `App.tsx:206` para o `SafeAreaView`; o `SafeAreaView` está hoje em `App.tsx:295` (ver 1.1) — referência de linha envelhecida.

### EndScreen.tsx (S5, fim de setlist)

**`onLayout` do `meio` — `EndScreen.tsx:97-103` e `114`**, mesma técnica do palco (comentário 94-96 diz que foi copiada dele).

[medido]
```
97:  const [meio, setMeio] = useState<{ largura: number; altura: number } | null>(null)
98:  const medirMeio = useCallback((e: LayoutChangeEvent) => {
100:    setMeio((m) => (m !== null && m.largura === w && m.altura === h ? m : { largura: w, altura: h }))
102:  const larguraBorda = meio === null ? 0 : Math.max(meio.largura * 0.15, touch.min)
103:  const alturaConteudo = meio === null ? 0 : Math.max(meio.altura, touch.min)
114:      <View style={styles.meio} onLayout={medirMeio}>
140:        {meio !== null ? (
142:            style={[styles.borda, { width: larguraBorda, height: alturaConteudo, left: 0 }]}
```
- Uso: **uma** zona de toque (esquerda, voltar à última) = 15% da largura do meio.
- **Reage à rotação: SIM** (onLayout).
- **Não reage**: a fileira de marcas "percorridas" usa a constante `FILEIRA = 900` (`EndScreen.tsx:57`) e a função `marcas(n)` calcula a largura de cada marca contra 900 fixos, sem ler a largura real:

[medido]
```
57:const FILEIRA = 900
58:const MARCA = { maxima: 34, piso: 6, altura: 3, raio: 2 }
59:const FOLGA = 5
61:function marcas(n: number): { largura: number; folga: number } | null {
...
64:  const cabe = (FILEIRA - FOLGA * (n - 1)) / n
```
Isto é uma largura fixa que **não** acompanha a janela (ver A2, S5).

### ModoDeReordenar.tsx (modo de reordenar)

**(a) `onLayout` do `ScrollView` — `ModoDeReordenar.tsx:596-598`**, lê **só a altura**:

[medido]
```
596:          onLayout={(e) => {
597:            rolagem.current.visivel = e.nativeEvent.layout.height
598:          }}
```
- Uso: limite da auto-rolagem durante o arrasto (`max = conteudo - visivel`, linha 328).
- Reage: sim (onLayout), mas **não lê largura**.

**(b) `measureInWindow` — `ModoDeReordenar.tsx:342-344`**, lê `y` e `h` do corpo, descarta `x` e `w` (`_x`, `_w`):

[medido]
```
342:      corpoRef.current?.measureInWindow?.((_x, y, _w, h) => {
343:        janela.current = { y, h }
344:      })
```
- Chamado a cada início de arrasto (`gesto.current.iniciar`), portanto lê o valor corrente no momento do toque. **Não lê largura.**

### App.tsx (raiz)

- `App.tsx:295`: `SafeAreaView` com `edges={['top','bottom','left','right']}` — consome os quatro insets; não lê largura, mas define a janela útil que o `onLayout` de S3/S5 enxerga.
- Nenhuma leitura de dimensão, nenhuma opção de orientação no navegador (grep vazio acima).

### Resumo da leitura de largura

| Arquivo:linha | O que lê | De onde | Reage a rotação? | Afeta layout? |
|---|---|---|---|---|
| StageScreen.tsx:212 | width, height | janela (`useWindowDimensions`) | sim (hook) | **não** — só log `rotation=` |
| StageScreen.tsx:471-477, 530 | largura, altura | View `meio` (`onLayout`) | sim | sim — bordas de toque 15% |
| EndScreen.tsx:97-103, 114 | largura, altura | View `meio` (`onLayout`) | sim | sim — borda de toque 15% |
| ModoDeReordenar.tsx:596 | só altura | ScrollView (`onLayout`) | sim | não (rolagem do arrasto) |
| ModoDeReordenar.tsx:342 | y, altura | corpo (`measureInWindow`) | lido a cada arrasto | não |

**Achado principal**: nenhuma tela escolhe layout por largura. Nenhuma faixa, nenhum breakpoint, nenhum `Dimensions.get`. Tudo o mais é literal fixo (A2). As únicas geometrias que acompanham a janela são as zonas de toque de 15% (S3 e S5).

## 1.3 Configuração de orientação — `app.json`

Não existe `app.config.*`:

[medido]
```
$ ls apps/native/app.config.*
(eval):1: no matches found: apps/native/app.config.*
```

[medido] `apps/native/app.json` inteiro:
```json
{
  "expo": {
    "name": "Octavia",
    "slug": "octavia",
    "version": "0.0.1",
    "orientation": "default",
    "android": {
      "package": "rocks.octavia.app"
    },
    "plugins": [
      "@config-plugins/react-native-blob-util",
      "@config-plugins/react-native-pdf",
      [ "expo-font", { "fonts": [ …6 arquivos de fonte… ] } ],
      "@react-native-community/datetimepicker"
    ]
  }
}
```
(o array de fontes foi abreviado em "…6 arquivos…"; nada mais omitido.)

- `orientation`: **`"default"`** (= livre, gira com o aparelho).
- `ios`: **a chave não existe** → `ios.supportsTablet` não declarado.
- `android.screenOrientation`: **não existe**.

[medido]
```
$ grep -n "ios\|supportsTablet\|screenOrientation" apps/native/app.json; echo "rc=$?"
rc=1
```

- `expo-build-properties` não é usado:

[medido]
```
$ grep -rn "expo-build-properties" apps/native/package.json apps/native/app.json package.json
(saída vazia, rc=1)
```

## 1.4 `targetSdkVersion` efetivo do Android e de onde vem

### O diretório `android/` não está no git

- Na worktree `octavia-n3-pre`: **não existe**.
- No checkout principal (`/Users/marcelviana/projects/octavia`, que está em `5f1c226`, atrás da origin/main): **existe no disco**, ignorado pelo git.

[medido]
```
$ ls -la apps/native/android            # na worktree n3-pre
ls: apps/native/android: No such file or directory

$ ls ../octavia/apps/native/android
app  build  build.gradle  gradle  gradle.properties  gradlew  gradlew.bat  settings.gradle

$ cd /Users/marcelviana/projects/octavia && git check-ignore -v apps/native/android; git ls-files apps/native/android | head
.gitignore:30:apps/native/android/	apps/native/android
(git ls-files vazio)

$ grep -n "android\|ios" .gitignore
30:apps/native/android/
31:apps/native/ios/
```

O CI gera o `android/` a cada build por `expo prebuild`:

[medido]
```
$ grep -n -i "prebuild\|gradlew\|assemble\|targetSdk\|expo-build-properties\|orientation" .github/workflows/*.yml
.github/workflows/native.yml:111:    - name: Prebuild (android)
.github/workflows/native.yml:112:      run: cd apps/native && npx expo prebuild --platform android --no-install
.github/workflows/native.yml:113:    - name: Gradle assembleDebug
.github/workflows/native.yml:114:      run: cd apps/native/android && ./gradlew assembleDebug --no-daemon
```

### A cadeia do valor

1. `app/build.gradle` só repassa `rootProject.ext`:

[medido] (`/Users/marcelviana/projects/octavia/apps/native/android/`)
```
$ grep -n -i "sdk\|targetSdk\|compileSdk\|minSdk" build.gradle app/build.gradle gradle.properties
app/build.gradle:100:    compileSdk rootProject.ext.compileSdkVersion
app/build.gradle:105:        minSdkVersion rootProject.ext.minSdkVersion
app/build.gradle:106:        targetSdkVersion rootProject.ext.targetSdkVersion
```
`build.gradle` (raiz) não define `ext` — só aplica `apply plugin: "expo-root-project"` (última linha 23-24 do arquivo). `gradle.properties` não tem `android.targetSdkVersion` (grep vazio para "sdk" nele).

2. O plugin `expo-root-project` define o `ext` a partir do catálogo de versões, com default 35 **só se o catálogo não tiver a chave**:

[medido] `node_modules/.pnpm/expo-modules-autolinking@57.0.12_typescript@6.0.3/node_modules/expo-modules-autolinking/android/expo-gradle-plugin/expo-autolinking-plugin/src/main/kotlin/expo/modules/plugin/ExpoRootProjectPlugin.kt`
```
53:  val minSdk = extra.setIfNotExist("minSdkVersion") { Integer.parseInt(versionCatalogs.getVersionOrDefault("minSdk", "24")) }
54:  val compileSdk = extra.setIfNotExist("compileSdkVersion") { Integer.parseInt(versionCatalogs.getVersionOrDefault("compileSdk", "35")) }
55:  val targetSdk = extra.setIfNotExist("targetSdkVersion") { Integer.parseInt(versionCatalogs.getVersionOrDefault("targetSdk", "35")) }
```

3. O catálogo é o `expoLibs`, ligado em `settings.gradle` por `expoAutolinking.useExpoVersionCatalog()`; ele lê `react-native/gradle/libs.versions.toml` e só é sobrescrito por `gradle.properties` `android.targetSdkVersion` (ausente):

[medido] `.../expo-autolinking-settings-plugin/src/main/kotlin/expo/modules/plugin/ExpoAutolinkingSettingsExtension.kt`
```
104:    val baseFile = if (reactNativeVersionCatalog != null) {
105:      File(reactNativeVersionCatalog)
106:    } else {
107:      File(
108:        reactNative,
109:        "gradle/libs.versions.toml"
…
119:      "android.targetSdkVersion" to "targetSdk",
…
125:        spec.create("expoLibs") { catalog ->
126:          catalog.from(catalogFile)
127:          properties.forEach { (propertyName, name) ->
128:            val property = settings.providers.gradleProperty(propertyName)
129:            if (property.isPresent) {
130:              catalog.version(name, property.get())
```

4. O número real:

[medido] `node_modules/.pnpm/react-native@0.86.3_@babel+core@7.28.4_@types+react@19.2.18_react@19.2.3/node_modules/react-native/gradle/libs.versions.toml`
```
3:minSdk = "24"
4:targetSdk = "36"
5:compileSdk = "36"
```
(idêntico no `node_modules` do checkout principal.)

**`targetSdkVersion` efetivo = 36**, origem `react-native@0.86.3/gradle/libs.versions.toml:4`, via catálogo `expoLibs` → `ExpoRootProjectPlugin.kt:55` → `app/build.gradle:106`. O default 35 do plugin não se aplica. Versões: `expo ~57.0.20`, `react-native 0.86.3` (`apps/native/package.json`).

5. Prova no artefato (APK debug de 2026-09-10 no checkout principal):

[medido]
```
$ ~/Library/Android/sdk/build-tools/36.0.0/aapt dump badging .../app/build/outputs/apk/debug/app-debug.apk | grep -E "^package|sdkVersion|targetSdk|supports-screens"
package: name='rocks.octavia.app' versionCode='1' versionName='0.0.1' platformBuildVersionName='16' platformBuildVersionCode='36' compileSdkVersion='36' compileSdkVersionCodename='16'
sdkVersion:'24'
targetSdkVersion:'36'
supports-screens: 'small' 'normal' 'large' 'xlarge'
```
Coerente com o já registrado: `docs/native/N0-PRECHECK.md:41` (N0-D3 "`compileSdk 36 / targetSdk 36 / minSdk 24`"), `docs/native/V1-PRECHECK.md:885`, `docs/native/V1-PR3-PRECHECK.md:145`.

### `AndroidManifest.xml` e `screenOrientation` da Activity

[medido] manifesto-fonte gerado pelo prebuild (`android/app/src/main/AndroidManifest.xml`, 2026-09-10):
```
21:    <activity android:name=".MainActivity" android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|uiMode|smallestScreenSize|assetsPaths" android:launchMode="singleTask" android:windowSoftInputMode="adjustResize" android:theme="@style/Theme.App.SplashScreen" android:exported="true" android:screenOrientation="unspecified">
```

[medido] manifesto mesclado (`app/build/intermediates/merged_manifests/debug/processDebugManifest/AndroidManifest.xml`):
```
4:android:minSdkVersion="24"
4:android:targetSdkVersion="36"
71:android:screenOrientation="unspecified"        ← rocks.octavia.app.MainActivity
125:android:screenOrientation="portrait"          ← expo.modules.devlauncher.launcher.errors.DevLauncherErrorActivity
145:android:screenOrientation="portrait"          ← com.google.mlkit.vision.codescanner.internal.GmsBarcodeScanningDelegateActivity
```
- A Activity do app: **`screenOrientation="unspecified"`** (tradução do `"orientation": "default"`), e `configChanges` inclui `orientation|screenSize|screenLayout|smallestScreenSize` → **o giro não recria a Activity**; o RN recebe a mudança de dimensão e re-renderiza (é o que faz `useWindowDimensions`/`onLayout` reagirem).
- Nenhum `resizeableActivity` declarado (grep sem ocorrência no mesclado).
- As duas activities `portrait` são de bibliotecas (tela de erro do dev launcher, delegate do scanner do ML Kit), não do app.

**Leitura para o N3 (não medida, conhecimento externo, marcar `[hipótese]` no documento)**: com `targetSdk 36` (Android 16), a plataforma passa a ignorar restrições de orientação/redimensionamento em telas grandes (sw ≥ 600 dp). Como o app já é `unspecified`, isso hoje não muda nada — mas significa que **travar em paisagem não seria uma saída disponível no tablet** a partir desse target, mesmo que alguém quisesse. Confirmar na documentação do Android antes de citar.

## 1.5 Síntese do A1

1. O app **gira livre** (`orientation: default` → `unspecified`), sem recriar a Activity.
2. **Nenhuma tela lê largura para escolher layout.** Leitura de largura com efeito em geometria: só as zonas de toque de 15% de S3 e S5 (via `onLayout`, reagem ao giro).
3. `useWindowDimensions` existe em um lugar só (S3) e serve só ao log `rotation=`.
4. S5 tem uma largura fixa de 900 (`FILEIRA`) que não acompanha a janela.
5. iOS: nada declarado (`supportsTablet` ausente).
6. `targetSdk` efetivo 36, vindo do catálogo do RN 0.86.3; `android/` é gerado e ignorado (existe só no checkout principal, de 2026-09-10).
