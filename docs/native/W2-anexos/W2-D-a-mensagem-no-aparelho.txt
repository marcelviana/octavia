================================================================================
W2-D — A MENSAGEM CRUA, MEDIDA NA TELA DO MÚSICO: ANTES × DEPOIS
================================================================================
Rastro de medição. 2026-09-15, AVD `octavia_tab32`, **modo avião conferido pelo
`ping`**. Estado: S3e (`arquivo não baixado`), música
`ux-audit-fase-d-cifra.pdf`, com o erro PROVOCADO tocando "Baixar" sem rede.
Zero downloads de bucket: as duas tentativas morreram na resolução de nome.

--------------------------------------------------------------------------------
§1. O QUE O MÚSICO LIA — ANTES
--------------------------------------------------------------------------------
Captura: `S3e-ANTES-a-mensagem-crua.png` (recorte 2× da região do
`download-erro`). Transcrito da imagem, verbatim, em quatro linhas:

  1786295844475-ux-audit-fase-d-cifra.pdf: Call to function
  'FileSystemDownloadTask.start' has been rejected.
  → Caused by: Unable to download a file: Unable to resolve host
  "mlxjmpbdchmwplcfislt.supabase.co": No address associated with hostname

  TRÊS COISAS ERRADAS, e a terceira ninguém tinha visto:
    1. inglês cru de biblioteca, numa tela em pt-BR (div. 125);
    2. o NOME DO OBJETO do bucket, que identifica o objeto e não a música;
    3. **o HOST do projeto Supabase** — que a higienização do `falha()` não
       pegou porque ela casa `https?://\S+` e ali o host vem NU, entre aspas,
       sem esquema. **Div. 137**, e vale para o log também.

  A (3) é **achado de segurança, não de forma**, e foi consertada nesta PR como
  EXTRA declarado ao escopo fechado, por ordem do Marcel no aval da entrega
  (2026-09-15): *"o host é o identificador do projeto Supabase, e relatório de log
  se cola em anexo — este repositório já tem três anexos de log commitados."*
  O `higienizar()` ganhou uma segunda alternativa para host entre aspas, e o
  limite dela vai DECLARADO (host de dois rótulos passa; exigir um ponto só
  comeria todo nome de arquivo entre aspas e destruiria a diagnosticabilidade que
  a separação das duas metades comprou). Três testes, um deles o controle
  negativo do próprio limite. Ver o commit 6.

  O W1 mediu só as duas primeiras linhas. Por quê: o `lc()` do protocolo de
  device é `logcat -d -s ReactNativeJS | grep -o 'OCTAVIA: .*'`, e o `grep -o`
  PARA NA QUEBRA DE LINHA. A mensagem tem duas linhas no logcat, e a segunda —
  a que carrega o host — nunca apareceu no relatório. **Div. 138.** Lida com
  `grep -A3`:

  $ adb -s emulator-5554 logcat -d -s ReactNativeJS | grep -A3 "download-error"
    I ReactNativeJS: OCTAVIA: download-error 1786295844475-ux-audit-fase-d-cifra.pdf: Call to function 'FileSystemDownloadTask.start' has been rejected.
    I ReactNativeJS: → Caused by: Unable to download a file: Unable to resolve host "mlxjmpbdchmwplcfislt.supabase.co": No address associated with hostname

--------------------------------------------------------------------------------
§2. O QUE O MÚSICO LÊ — DEPOIS
--------------------------------------------------------------------------------
Captura: `S3e-DEPOIS-a-frase-do-musico.png`. E desta vez o `uiautomator dump`
também a expõe, porque é UMA linha curta:

$ python3 — extrai `text` de todos os nós de `DEPOIS-08-S3e-erro-escuro.xml`
    'AVULSA'
    'UX-AUDIT SHOW PADRÃO'
    'ux-audit-fase-d-cifra.pdf · Unknown Artist · Cifra'
    'arquivo não baixado'
    'ux-audit-fase-d-cifra.pdf · cifra (1 KB) não está neste aparelho. Sem conexão agora — toque em Baixar quando a rede voltar.'
    'não consegui baixar'          <- o nó `download-erro`
    'Baixar'

E o LOG, no mesmo toque, continua com tudo:

    OCTAVIA: download-error 1786295844475-ux-audit-fase-d-cifra.pdf: Call to function 'FileSystemDownloadTask.start' has been rejected.
    → Caused by: Unable to download a file: Unable to resolve host "mlxjmpbdchmwplcfislt.supabase.co": No address associated with hostname

  As duas metades separadas, que era o ponto: a tela ficou com a frase, o log
  ficou com o diagnóstico. **Nenhuma linha `log(` mudou** — G3 57 = 57.

--------------------------------------------------------------------------------
§3. O A20 COMO ACEITE — VARREDURA DO QUE AS TELAS MOSTRARAM
--------------------------------------------------------------------------------
Não o `gate:a20` (que lê literal no código), mas o ACEITE: toda cadeia de `text`
e `content-desc` dos dumps, varrida contra o vocabulário do a20 mais
`rejected`, `unable`, `call to function` e `host`.

  DEPOIS:  128 cadeias distintas · 4 acusações
  ANTES :  119 cadeias distintas · 4 acusações

  E as 4 são as MESMAS nos dois, todas a mesma coisa:
    'ux-audit-fase-d-cifra.pdf · Unknown Artist · Cifra'   <- termo "unknown"
    'Unknown Artist' (×3, em texto e content-desc)

  `Unknown Artist` é **DADO**, não literal do app: está assim no `content.json`
  da conta de audit (`"artist": "Unknown Artist"`), medido no aparelho. O app
  renderiza o campo. Não há literal de UI em inglês no app. **A20 ✓.**

  >> E AQUI MORA A DIV. 139, que é o achado mais desconfortável desta PR.
  >> A varredura sobre os dumps do ANTES acusa as MESMAS QUATRO — ou seja, ela
  >> NÃO VÊ a frase de quatro linhas que é o objeto inteiro da div. 125. Por quê:
  >> o `<Text>` multilinha do `download-erro` volta do `uiautomator dump` com
  >> `text=""` e só os `bounds`:
  >>
  >>   resource-id : download-erro
  >>   text        : ''
  >>   content-desc: ''
  >>   bounds      : [650,572][1910,749]
  >>
  >> Três instrumentos em fila, e nenhum enxergou a mesma cadeia: o `gate:a20`
  >> por NATUREZA (div. 130 — é valor de tempo de execução, não literal); o
  >> `uiautomator dump` por RENDERIZAÇÃO (div. 139); e o `lc()` do protocolo por
  >> TRUNCAMENTO (div. 138). O que a viu foi a **captura de tela** e o
  >> **logcat lido inteiro** — e o que passa a IMPEDI-LA é o teste de unidade do
  >> commit 4, que não depende de nenhum dos três.

--------------------------------------------------------------------------------
§4. O TESTE, E O CONTROLE NEGATIVO DELE
--------------------------------------------------------------------------------
`apps/native/test/files-mensagem.test.ts`, 7 casos, roda no projeto `native` do
Vitest — logo **no CI, em toda PR** (div. 129: os cinco gates de script não).

A lista de frases está escrita NO TESTE, não importada do `files.ts`: um teste
que importasse a lista aprovaria qualquer frase que alguém acrescentasse lá.

CONTROLE NEGATIVO — com o ramo 3 do `falha()` devolvido ao que era:

  × nenhuma mensagem crua de biblioteca chega à tela
  × o nome do objeto do bucket NÃO vai para a tela — vai para o log
  × o que não é `Error` também não vaza
  AssertionError: "Call to function 'FileSystemDownloadTask.start' has been
  rejected." → "1751910900697-Easy_-_Guitar.pdf: Call to function
  'FileSystemDownloadTask.start' has been rejected."
  Tests  3 failed | 29 passed (32)

  A cadeia da falha é, verbatim, a que o W1 mediu no aparelho.
  Restaurado o conserto: 32 passados, 4 arquivos.
