# W3 — anexos

Rastro de medição da W3 (`w3/gates-no-ci`), a PR que faz os gates **impedirem**
em vez de só medirem. Commitados junto com o código que eles medem.

| arquivo | o que traz |
|---|---|
| `W3-A-o-coletor-e-a-reprovacao-falsa.txt` | a div. 136 medida até o fim — a menção em comentário que vira **reprovação falsa** (div. 140), os quatro controles negativos do conserto, e a prova de que a população não muda |
| `W3-B-os-cinco-gates-e-o-ci.txt` | a tabela dos cinco: onde cada um roda depois desta PR, o que fica de fora e por quê; a prova LOCAL **e a prova NO CI** de que um commit que reprova um gate fica vermelho (PR #304, fechada sem merge) |
| `W3-C-a-rede-de-seguranca.txt` | a div. 137, segunda metade: o que escapa do `falha()`, como se alcança, e o CN com a higienização de ontem reposta |
| `W3-D-ci-e-contabilidade.txt` | a faixa do `native.yml` com a 15ª corrida; contabilidade de prod, bucket, aparelho e emulador |

**Divergências desta PR: 140 e 141.** Continuando de 139 (W2).

A fonte do bloco é o **`W3-ENCERRAMENTO.md`**, commitado ao lado desta pasta.
