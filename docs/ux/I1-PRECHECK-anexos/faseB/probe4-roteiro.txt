# [commit 3a] SUBSTITUÍDO pelo probe4.ts (I1-D35: roda o Marcel, contra prod — I1-D34). Roteiro mantido como rastro.
# probe 4 — toasts das setlists (H-I1-5). ESTADO: NÃO EXECUTADO — bloqueado (bloqueio.txt, div. 509).
# Escrita declarada ANTES (lista fechada): 1 POST /api/setlists (I1-probe4) · 1 PUT /api/setlists/<id> · 1 DELETE
#   /api/setlists/<id> (aba 1). O DELETE da aba 2 dá 404 e não escreve. Nada da conta principal.
# Passos: criar "I1-probe4" → screenshot (toast? diálogo fechou?) → renomear → screenshot → abrir 2ª aba em /setlists
#   → apagar na 1ª → screenshot → apagar na 2ª (404) → screenshot (diálogo ficou aberto? algum texto?)
#   → GET /api/setlists prova que I1-probe4 não existe.
# Esperado pela leitura: nenhum toast em nenhum passo (setlist-manager.tsx:7 × app/layout.tsx:127); no 404, o diálogo
#   não fecha (setlist-manager.tsx:140-157).
# Saída: probe4-requests.txt, probe4-passo{1..5}.png (recorte do diálogo/lista, sem conteúdo de música).
